/**
 * Soglia di scroll (px) oltre la quale la Navbar attiva lo stato "scrolled".
 * Rif. Fase 05.1.
 */
export const NAVBAR_SCROLL_THRESHOLD = 24;

/**
 * Breakpoint (px) considerato "desktop" per la logica dei componenti
 * (coerente con il breakpoint `md` di Tailwind, 768px).
 */
export const DESKTOP_BREAKPOINT = 768;
